import { TicketOptionsDialogDirective } from './ticket-options-dialog.directive';

describe('TicketOptionsDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketOptionsDialogDirective();
    expect(directive).toBeTruthy();
  });
});
