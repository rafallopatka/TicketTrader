import { SvgMapDirective } from './svg-map.directive';

describe('SvgMapDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgMapDirective();
    expect(directive).toBeTruthy();
  });
});
